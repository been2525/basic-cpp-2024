// project19.cpp : 이 파일에는 'main' 함수가 포함됩니다. 거기서 프로그램 실행이 시작되고 종료됩니다.
//

#include <iostream>

void func()
{
	//.....
}

int main()
{

	func();
	return 0;
}

const int mum = 1000; // 상수화 선언할때는 이렇게 해야함

//ClassName objName;
//ClassName *p = new ClassName; // new 연산자는 힙영역에 메모리를 할당하고 그 주소를 리턴한다.

char a = 'a';
int* p = &a;
